import { ArrowRight, BriefcaseBusiness, ExternalLink, HeartHandshake, Sparkles, Search, RefreshCw } from "lucide-react";
import { useEffect, useState } from "react";
import { useLocation } from "wouter";
import HanaGameFrame from "@/components/HanaGameFrame";

type Opportunity = { title: string; description: string; url: string; why: string; source?: string };
const sources: Opportunity[] = [
  { title: "Devpost Hackathons", description: "Browse current online and in-person hackathons and filter by technology or theme.", url: "https://devpost.com/hackathons", why: "Strong place to find build-focused competitions.", source: "Devpost" },
  { title: "Google for Developers", description: "Follow Google's developer events, programs, challenges, and learning opportunities.", url: "https://developers.google.com/community", why: "Useful for Google ecosystem events and developer communities.", source: "Google" },
  { title: "GitHub Explore", description: "Discover projects and communities where you can practise open-source engineering.", url: "https://github.com/explore", why: "Useful for visible engineering experience.", source: "GitHub" },
  { title: "Major League Hacking", description: "Explore student hackathons and developer community opportunities.", url: "https://mlh.io/seasons/2026/events", why: "Useful for student-focused hackathon discovery.", source: "MLH" },
];

export default function Opportunities() {
  const [, navigate] = useLocation();
  const [query, setQuery] = useState(""); const [items, setItems] = useState<Opportunity[]>(sources); const [loading, setLoading] = useState(false); const [updated, setUpdated] = useState(false);
  const find = async () => { setLoading(true); try { const q = query.trim() || "latest hackathons competitions scholarships programs for computer science students"; const r = await fetch(`/api/opportunities?q=${encodeURIComponent(q)}`); const d = await r.json() as { opportunities?: Opportunity[] }; if (d.opportunities?.length) setItems(d.opportunities); setUpdated(true); } catch { setItems(sources); } finally { setLoading(false); } };
  useEffect(() => { void find(); }, []);
  return <HanaGameFrame title="Opportunities · Find your opening">
    <section className="rounded-[30px] border border-white/10 bg-white/[.055] p-5 shadow-xl backdrop-blur sm:p-6"><div className="flex items-start gap-3"><Search className="h-6 w-6 text-[#f1c77b]"/><div className="flex-1"><p className="text-xs font-bold uppercase tracking-[.16em] text-white/40">Live opportunity scout</p><h2 className="mt-1 font-display text-2xl font-semibold">Find what is actually available now.</h2><p className="mt-2 text-sm leading-6 text-white/50">Hana can browse current opportunities and recommend only the strongest matches for your direction.</p><div className="mt-4 flex flex-col gap-3 sm:flex-row"><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==='Enter'&&void find()} placeholder="AI hackathons, Google programs, cybersecurity competitions…" className="flex-1 rounded-2xl border border-white/10 bg-black/10 px-4 py-3 text-white outline-none placeholder:text-white/30"/><button onClick={()=>void find()} disabled={loading} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-[#f1c77b] px-5 py-3 font-bold text-[#172630]"><RefreshCw className={`h-4 w-4 ${loading?'animate-spin':''}`}/>{loading?"Checking…":"Find latest"}</button></div></div></div>{updated&&<p className="mt-3 text-xs text-white/35">Live scout refreshed from web sources. Always verify deadlines and eligibility on the official page.</p>}</section>
    <section className="mt-5 grid gap-4 md:grid-cols-2">{items.map(item=><article key={item.url} className="rounded-[28px] border border-white/10 bg-white/[.055] p-5 shadow-xl backdrop-blur sm:p-6"><div className="flex items-center justify-between"><div className="rounded-2xl bg-[#f1c77b] p-3 text-[#172630]"><BriefcaseBusiness className="h-5 w-5"/></div><HeartHandshake className="h-5 w-5 text-[#db8b71]"/></div><p className="mt-4 text-xs font-bold uppercase tracking-[.14em] text-white/35">{item.source ?? "Web source"}</p><h2 className="mt-2 font-display text-2xl font-semibold">{item.title}</h2><p className="mt-2 text-sm leading-6 text-white/55">{item.description}</p><div className="mt-4 rounded-2xl border border-white/10 bg-black/10 p-3 text-sm text-white/65"><strong className="text-white">Why Hana recommends it:</strong> {item.why}</div><div className="mt-5 flex flex-wrap gap-2"><a href={item.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full bg-[#f1c77b] px-4 py-2.5 text-sm font-bold text-[#172630]">Open official source <ExternalLink className="h-4 w-4"/></a><button onClick={()=>navigate(`/chat?prompt=${encodeURIComponent(`Research this opportunity deeply: ${item.title} ${item.url}. Check current deadline, eligibility, location/online status, theme, required skills, and whether it fits my career path. Recommend whether I should pursue it.`)}`)} className="inline-flex items-center gap-2 rounded-full border border-white/10 px-4 py-2.5 text-sm font-semibold text-white/70"><Sparkles className="h-4 w-4"/>Ask Hana to check fit</button></div></article>)}</section>
    <section className="mt-5 rounded-[30px] border border-white/10 bg-white/[.055] p-6 shadow-xl backdrop-blur sm:p-7"><p className="text-xs font-bold uppercase tracking-[.16em] text-white/40">Hana opportunity mode</p><h2 className="mt-2 font-display text-2xl font-semibold">Don't just collect links. Decide.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-white/50">Ask Hana to compare current opportunities by deadline, eligibility, effort, learning value, portfolio value, and fit with your roadmap. Hana should verify changing details from current sources before recommending one.</p><button onClick={()=>navigate(`/chat?prompt=${encodeURIComponent("Browse the latest opportunities for my selected career path. Find hackathons, Google or other major developer programs, competitions, scholarships, and open-source opportunities. Verify current details from official sources, rank the strongest few by fit, and explain why each is worth considering.")}`)} className="mt-5 inline-flex items-center gap-2 rounded-full bg-[#f1c77b] px-5 py-3 font-bold text-[#172630]">Ask Hana for latest recommendations <ArrowRight className="h-4 w-4"/></button></section>
  </HanaGameFrame>;
}
